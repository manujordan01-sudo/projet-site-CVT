MONGO_URI=mongodb://localhost:27017/vente-auto
JWT_SECRET=change_this_secret
UPLOAD_DIR=uploads
PORT=5000
OPENAI_API_KEY=
# Optional: specify model (defaults to gpt-3.5-turbo). Change to gpt-4 if available.
OPENAI_MODEL=gpt-3.5-turbo
# Optional: protect the AI proxy with a simple token — client must send header 'x-ai-key'
AI_PROXY_TOKEN=
